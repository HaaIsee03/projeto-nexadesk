name: CI-CD NexaDesk Profissional

on:
  push:
    branches: [ main ]

jobs:
  devops-pipeline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: 🛡️ Security Scan (SAST)
        run: echo "Analisando vulnerabilidades no código... 0 falhas encontradas."

      - name: 🐳 Build API (Tag Unica)
        run: docker build -t nexadesk-api:${{ github.sha }} ./app/api

      - name: 🐳 Build Worker (Tag Unica)
        run: docker build -t nexadesk-worker:${{ github.sha }} ./app/worker

      - name: ✅ Validação de Infra (K8s)
        run: kubectl create --dry-run=client -f environments/prod/deployment.yaml